# Jekyll build output & caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local Ruby deps
vendor/
Gemfile.lock

# private working docs / local notes (project standard — never committed)
.workspace/
