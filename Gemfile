# Local development only.
# GitHub Pages builds the site with its own pinned environment and
# ignores this file, so it's just here so you can preview locally.
source "https://rubygems.org"
gem "jekyll", "~> 4.3"
